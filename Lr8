#Написать программу, которая считывает с клавиатуры 
#предложения и выводит только те, которые не содержат запятых
print('Введите предложения (через точку): ')
str = input()
#перебираем все предложения
for s in str.split('.'):
    flag = True
    #перебор всех символов в каждом предложении
    for i in range(0, len(s)):
        if s[i] == ',':
            #если в предложении есть запятая, изменяем флаг
            flag = False
    #если запятых нет
    if flag == True:
        print(s)
